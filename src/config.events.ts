// Events that should not be displayed in the events list
export const excludedEvents = ['Tentative', 'Private', 'Plenum'];

// Map of keywords to icons for event categorization
export const eventIconMap: Record<string, string> = {
  Workshop: '🔧',
  Hackathon: '💻',
};
